

services:
  toolbox:
    container_name: my_toolbox
    build: .
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TZ=America/Edmonton
    restart: unless-stopped
    ports:
      - "9000:9000"